version: 2
jobs:
  build:
    working_directory: ~/spawny
    docker:
      # see the dockerfile
      - image: bu5hm4n/spawny:meson
    steps:
      - checkout
      - run:
          name: clone git —recursive
          command: |
            git submodule update --init --recursive
      - run:
          name: ninja & ninja test
          command: |
            mkdir build
            cd build
            meson ..
            ninja all
            ninja test
